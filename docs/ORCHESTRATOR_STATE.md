# Orchestrator State

- Current milestone: M1 — offline note foundations.
- Current branch: `feat/chrome-notes` in `/home/ashfame/git-worktrees/pageperch-chrome-notes`.
- Last accepted commit: `68b75be` (`feat(identity): add canonical page identity`); PP-003A is accepted and awaiting its commit.
- Current task: PP-003B — implement local-first `NoteService`.
- Completed tasks: Imported and pushed the governing inputs; bootstrapped durable project state; delivered the reproducible audited MV3 foundation; implemented canonical page identity; implemented versioned note/settings models, storage-neutral repositories, recovery-safe Chrome local adapters, exact-origin indexes, strict schema/key/URL validation, settings v0→v1 migration, defensive copies, and serialized cross-context mutations.
- Next 3 tasks: Implement `NoteService` save/delete/index behavior; integrate the per-page restricted block editor and origin index; add identity settings and collision-safe migrations.
- Key architecture decisions: Offline-first Chrome local storage is authoritative; page identity is exact-origin scoped; remote records are one deterministic S3 JSON object per page key; UI, application, persistence, and transport boundaries remain separate.
- Open risks and blockers: A crash between explicit physical note removal and its index cleanup can leave a filtered dangling membership until physical delete is retried; without Web Locks the mutation fallback is realm-local, but target Chrome exposes cross-context Web Locks; the pinned editor emits a stale React peer warning and 58 moderate production dependency advisories with no compatible automated fix; five high advisories exist only in the ESLint development chain and require an ESLint 10 compatibility assessment; npm reports an unapproved development install script for `@parcel/watcher`; real toolbar-to-native-side-panel host automation remains an explicit Playwright capability skip; BYOS production OAuth requires a registered client ID and interactive approval; public distribution remains blocked on a GPL-compatible project-license decision.
- Commands that currently pass: Clean `npm ci`; `npm run check` including format, lint, strict typecheck, 223 unit/component/repository/audit tests at 96.50% statement and 91.70% branch coverage, deterministic production build, 13-file extension package audit, genuine two-file isolated-editor production/CSP audit, and unpacked Chromium smoke with one pass; 106 focused repository cases plus 84 focused page-identity cases; `git diff --check`.
- Commands that currently fail: `npm audit --omit=dev` exits nonzero for 58 moderate advisories propagated by the mandated isolated editor; full `npm audit` additionally reports five high development-only advisories through ESLint/minimatch/brace-expansion; the toolbar-to-native-side-panel Playwright case is intentionally skipped because Playwright cannot drive extension chrome.
- Context handoff summary: PP-003A passed three review cycles and main-thread full verification. Note records use one versioned storage key plus exact-origin index envelopes; owned malformed/future/inconsistent values raise typed recovery errors without mutation, while true absence and dangling members remain harmless. Canonical SHA-256 keys require 42 base64url characters plus terminal `[AEIMQUYcgkosw048]`. Mutation coordination uses a shared per-storage queue and cross-context Web Lock. After committing PP-003A, dispatch only PP-003B local-first note service to one worker, then one reviewer.
