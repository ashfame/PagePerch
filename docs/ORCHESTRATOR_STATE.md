# Orchestrator State

- Current milestone: M1 — offline note foundations.
- Current branch: `feat/chrome-notes` in `/home/ashfame/git-worktrees/pageperch-chrome-notes`.
- Last accepted commit: `37483be` (`feat(storage): add versioned local repositories`); PP-003B is accepted and included in the next atomic commit.
- Current task: PP-004 — add the per-page restricted block editor and exact-origin root index.
- Completed tasks: Imported and pushed the governing inputs; bootstrapped durable project state; delivered the reproducible audited MV3 foundation; implemented canonical page identity; implemented versioned note/settings models, storage-neutral repositories, recovery-safe Chrome local adapters, exact-origin indexes, strict schema/key/URL validation, settings v0→v1 migration, defensive copies, serialized cross-context mutations, and the validated local-first note service with normalized Gutenberg hashing, unchanged-save suppression, logical tombstones, resurrection, deterministic recent-note ordering, and same-page mutation sequencing.
- Next 3 tasks: Integrate the per-page restricted block editor, active-tab navigation, autosave states, and root-origin index; add identity settings and collision-safe migrations; implement BYOS OAuth and in-memory credential lifecycle.
- Key architecture decisions: Offline-first Chrome local storage is authoritative; page identity is exact-origin scoped; remote records are one deterministic S3 JSON object per page key; UI, application, persistence, and transport boundaries remain separate.
- Open risks and blockers: A crash between explicit physical note removal and its index cleanup can leave a filtered dangling membership until physical delete is retried; without Web Locks the mutation fallback is realm-local, but target Chrome exposes cross-context Web Locks; repository validation checks content-hash encoding rather than recomputing its correspondence to HTML, while service writes compute the canonical digest; the real editor must confirm untouched and cleared serialization in PP-004; the pinned editor emits a stale React peer warning and 58 moderate production dependency advisories with no compatible automated fix; five high advisories exist only in the ESLint development chain and require an ESLint 10 compatibility assessment; npm reports an unapproved development install script for `@parcel/watcher`; real toolbar-to-native-side-panel host automation remains an explicit Playwright capability skip; BYOS production OAuth requires a registered client ID and interactive approval; public distribution remains blocked on a GPL-compatible project-license decision.
- Commands that currently pass: Clean `npm ci`; `npm run check` including format, lint, strict typecheck, 289 unit/component/repository/audit tests at 96.18% statement and 92.16% branch coverage, deterministic production build, 13-file extension package audit, genuine two-file isolated-editor production/CSP audit, and unpacked Chromium smoke with one pass; 159 focused note-service and affected repository cases, 106 focused repository cases, and 84 focused page-identity cases; `git diff --check`.
- Commands that currently fail: `npm audit --omit=dev` exits nonzero for 58 moderate advisories propagated by the mandated isolated editor; full `npm audit` additionally reports five high development-only advisories through ESLint/minimatch/brace-expansion; the toolbar-to-native-side-panel Playwright case is intentionally skipped because Playwright cannot drive extension chrome.
- Context handoff summary: PP-003B passed corrective review and main-thread full verification. `DefaultNoteService` revalidates the complete canonical identity and digest, sanitizes same-origin representative URLs, trims/falls back titles, normalizes line endings and the standard empty Gutenberg paragraph, hashes canonical content, awaits local persistence, and hides tombstones from loads/indexes. Actual changes alone advance injected time/revision values; existing clears create indefinite tombstones; same-page mutations serialize and recover after rejected writes. PP-004 must integrate the actual isolated editor with `allowApi: false`, confirm its empty serialization, flush pending saves before page-key remounts, expose explicit local/sync/error states, and render the recent exact-origin index only on root identities.
