# Orchestrator State

- Current milestone: M1 — offline note foundations.
- Current branch: `feat/chrome-notes` in `/home/ashfame/git-worktrees/pageperch-chrome-notes`.
- Last accepted commit: `1aa10d3` (`chore(extension): scaffold manifest v3 foundation`); PP-002 is accepted and awaiting its commit.
- Current task: PP-003 — implement versioned local repositories and `NoteService`.
- Completed tasks: Imported and pushed the governing inputs; bootstrapped durable project state; delivered the reproducible audited MV3 foundation; implemented storage-neutral canonical page identity with explicit unsupported results, exact-origin exclusions, deterministic query semantics, root detection, and SHA-256/base64url keys.
- Next 3 tasks: Implement versioned local repositories and `NoteService`; integrate the per-page restricted block editor and origin index; add identity settings and collision-safe migrations.
- Key architecture decisions: Offline-first Chrome local storage is authoritative; page identity is exact-origin scoped; remote records are one deterministic S3 JSON object per page key; UI, application, persistence, and transport boundaries remain separate.
- Open risks and blockers: The pinned editor emits a stale React peer warning through `react-autosize-textarea`; its WordPress graph accounts for 58 moderate production dependency advisories with no compatible automated fix; five high advisories exist only in the ESLint development chain and require an ESLint 10 compatibility assessment; npm reports an unapproved development install script for `@parcel/watcher`; real toolbar-to-native-side-panel host automation is unavailable in Playwright and remains an explicit diagnostic skip; BYOS production OAuth requires a registered client ID and interactive approval; public distribution remains blocked on a GPL-compatible project-license decision.
- Commands that currently pass: Clean `npm ci`; `npm run check` including format, lint, strict typecheck, 117 unit/component/audit tests at 98.31% statement and 91.08% branch coverage, deterministic production build, 13-file extension package audit, genuine two-file isolated-editor production/CSP audit, and unpacked Chromium smoke with one pass; focused page-identity suite with 84 tests; `git diff --check`.
- Commands that currently fail: `npm audit --omit=dev` exits nonzero for 58 moderate advisories propagated by the mandated isolated editor; full `npm audit` additionally reports five high development-only advisories through ESLint/minimatch/brace-expansion; the toolbar-to-native-side-panel Playwright case is intentionally skipped because Playwright cannot drive extension chrome.
- Context handoff summary: PP-002 passed independent review plus main-thread focused and full verification. `DefaultPageIdentityService` follows WHATWG URL normalization, filters frozen built-ins and caller-provided exact-origin rules case-insensitively, code-unit sorts decoded query names/values, reserializes through `URLSearchParams`, and hashes the exact canonical UTF-8 URL. Invalid custom setting names remain a PP-005 validation responsibility. After committing PP-002, dispatch only PP-003 local repositories and note service to one worker, then one reviewer.
