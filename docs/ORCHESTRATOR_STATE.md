# Orchestrator State

- Current milestone: M14 — editor top gutter and behavioral test boundaries, completed; M15 release pending.
- Current branch: `feat/chrome-notes` in `/home/ashfame/git-worktrees/pageperch-chrome-notes`.
- Last accepted implementation commit: `01639f8` (`fix(editor): preserve unsupported paste text`).
- Current task: PP-020 — commit and push the accepted top-gutter and behavior-first test correction, confirm implementation CI, then bind it to `v0.1.6`.
- Completed tasks: All implementation-plan delivery batches PP-001 through PP-012 are complete, including the audited MV3 foundation, offline page identity and notes, control-free content-growing Gutenberg editor, default-off opt-in exact-origin index, collision-stacking identity exclusions, established-service BYOS OAuth/S3 integration, automatic queue-based reconciliation with passive status, real packaged-browser keyboard/history/persistence/index coverage, complete setup/privacy/operations documentation, and the deterministic recovery-safe release artifact/checksum flow. PP-013 produced `v0.1.2`, whose editor acceptance is rejected. PP-014 supersedes that implementation with exact direct Gutenberg dependencies, injected content styles, trusted structured paste, selection-safe root adaptation, and strict full-height evidence and was independently approved after one corrective review. PP-015 binds that state to `v0.1.3`, whose manual editor interaction is rejected. PP-016 restores pointer typing, removes the two pinned Gutenberg deprecations, exposes the runtime version in settings, and was independently approved. PP-017 binds the accepted correction to the audited `v0.1.4` minor release. PP-018 restores left-flowing blocks, removes writing-canvas selection chrome, and adds safe occurrence-preserving source-HTML fallback for unsupported paste after one rejected review and one accepted corrective pass. PP-019 binds the accepted correction to the deterministic audited `v0.1.5` minor release. PP-020 adds the requested 16-pixel root top gutter and replaces appearance-coupled tests with behavioral evidence after one corrective review pass.
- Next 3 tasks: Commit and push PP-020; confirm implementation CI before release metadata changes; cut and verify the deterministic annotated `v0.1.6` minor release.
- Key architecture decisions: Offline-first Chrome local storage is authoritative; page identity is exact-origin scoped; remote records are one deterministic S3 JSON object per page key; the durable revision-specific queue entry is the sole per-page unsynced flag; synchronization is automatic with no manual remote control; the side-panel editor is control-free but keyboard complete; the root recent-note index is a default-off preference that performs no work while disabled; UI, application, persistence, and transport boundaries remain separate.
- Open risks and blockers: A crash between explicit physical note removal and its index cleanup can leave a filtered dangling membership until physical delete is retried; without Web Locks the mutation fallback is realm-local, but target Chrome exposes cross-context Web Locks; repository validation checks content-hash encoding rather than recomputing its correspondence to HTML, while service and migration writes compute the canonical digest; a storage implementation could theoretically persist phase data without the journal marker even though current Chromium storage batches one `set` call, so phase-ahead recovery remains a hardening consideration; a migration can begin after sync preparation and temporarily allow a pre-migration source upload between individually locked phases, with automatic completion follow-up and later full reconciliation providing convergence; disconnect resumes a legitimate pending identity journal before removing its duplicated token, while a malformed journal intentionally blocks automatic disconnect until recovery; an already-issued S3 request cannot be physically revoked at disconnect, but worker generation, connection-bound credential acquisition, settings CAS, and subsequent preflights prevent further stale operations or metadata; a cross-context core-settings change can safely make a stale identity request fail until the options page reloads; active-session verification intentionally derives and hashes identity twice per navigation and cross-window activation may emit a redundant same-key state; cleanup failures deliberately retain the current draft registration until explicit retry, while runtime failures follow page transfer automatically; the security-sensitive stored-block sanitizer requires adversarial regression coverage as supported markup evolves; the pinned editor emits a stale React peer warning and 60 moderate production dependency advisories with no compatible automated fix; five high advisories exist only in the ESLint development chain and require an ESLint 10 compatibility assessment; npm reports an unapproved development install script for `@parcel/watcher`; the side-panel entry is 3,985,325 raw bytes and the complete built JavaScript package is 1,147,958 gzip-compressed bytes against the 5 MiB budget; visual theme, contrast, and visible-focus appearance are manual acceptance concerns rather than exact-presentation automated contracts; real toolbar-to-native-side-panel host automation remains an explicit Playwright capability skip; production OAuth requires an approved public client ID and interactive consent.
- Commands that currently pass: Clean `npm ci`; PP-018 implementation CI run `30196860765`; `npm run check` on the accepted PP-020 `0.1.5` source including format, lint, strict typecheck, 973 unit/component/repository/audit/release tests at 91.45% statement and 87.39% branch coverage, deterministic production build, 16-file extension package audit, genuine two-file isolated-editor production/CSP audit, and unpacked Chromium smoke with seven passes and one explicit capability skip; exact repeated/mixed-order/concealed/malformed unsafe-text repros; trusted unsupported table/figure/image fallback with live/storage/reload occurrence and order checks; rendered 16-pixel top/left/right writing-column insets at narrow and wide widths without horizontal overflow; real paragraph/heading/list-item focus-edit-undo behavior; a dedicated upper-right/middle-left/lower-right canvas click-to-focus-before-type flow with local persistence and reload; absence of both rejected WordPress 6.5 deprecations; exact fail-closed compatibility-transform coverage; runtime-manifest settings version; direct serialized-block parser recognition; genuine trusted Chromium `text/html` copy/paste for headings, paragraphs, lists, quotes, code, safe inline formatting, partial selections, and nested list roots; native undo/redo; sanitized persistence and reload; strict 280×720 short-note viewport fill with visible status; long document growth without an inner editor scroller; local persistence through panel/browser restart; default-off then opted-in exact-origin index; 52 focused version/release cases; real concurrent packager rejection/coherence; prior deterministic `0.1.5` release evidence; `git diff --check`.
- Commands that currently fail: `npm audit --omit=dev` exits nonzero for 60 moderate advisories propagated by the mandated isolated editor; full `npm audit` additionally reports five high development-only advisories through ESLint/minimatch/brace-expansion; the toolbar-to-native-side-panel Playwright case is intentionally skipped because Playwright cannot drive extension chrome.
- Context handoff summary: After `v0.1.5`, the user requested a 16-pixel root top gutter and rejected tests coupled to exact element styling. PP-020 adds the gutter, deletes the static CSS/theme baseline and selector/token assertions, removes exact computed colors/borders/outlines/shadows/margins/fonts/transitions and internal-class/style-object checks, and replaces them with rendered geometry plus real focus/edit/undo, semantic paste, persistence, viewport-fill, growth, and overflow behavior. Independent review requested removal of three remaining implementation-only assertions; one corrective pass closed them. The complete main gate passes 973 tests, build/audits, and Chromium 7/1. Commit and implementation CI must pass before PP-021 changes version metadata for `v0.1.6`. Native toolbar hosting, visual appearance, and approved live BYOS remain explicit manual boundaries; prior release tags remain untouched.
