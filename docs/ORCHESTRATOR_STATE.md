# Orchestrator State

- Current milestone: M1 — offline note foundations.
- Current branch: `feat/chrome-notes` in `/home/ashfame/git-worktrees/pageperch-chrome-notes`.
- Last accepted commit: `055611f` (`feat(editor): add restricted note editor adapter`); PP-004C2A is accepted and included in the next atomic commit.
- Current task: PP-004C2B — compose cached note loading and the accepted restricted editor into the side panel, surface explicit local save states, and replace the temporary no-pending-state flush boundary.
- Completed tasks: Imported and pushed the governing inputs; bootstrapped durable project state; delivered the reproducible audited MV3 foundation; implemented canonical page identity; implemented versioned local models/repositories; implemented the validated local-first note service; implemented the UI-neutral active-page session controller; composed narrow Chrome tabs/settings adapters with a StrictMode-safe accessible React shell; implemented an independently reviewed isolated-editor adapter with exact block modes, safe stored-content normalization, first-edit capture, enforced local-only WordPress API handling, mode-keyed remounts, and accessible automatic themes; and implemented an independently reviewed cached-note draft controller with exact 750 ms actual-change debounce, serial local persistence, retryable state, lossless awaited stop, and a generation-aware navigation flush coordinator.
- Next 3 tasks: Mount the cached draft controller and real editor with actionable load/editor/save states while atomically replacing the no-op flush; render the recent exact-origin index on root identities with safe new-tab navigation; complete identity settings and deterministic note migrations.
- Key architecture decisions: Offline-first Chrome local storage is authoritative; page identity is exact-origin scoped; remote records are one deterministic S3 JSON object per page key; UI, application, persistence, and transport boundaries remain separate.
- Open risks and blockers: A crash between explicit physical note removal and its index cleanup can leave a filtered dangling membership until physical delete is retried; without Web Locks the mutation fallback is realm-local, but target Chrome exposes cross-context Web Locks; repository validation checks content-hash encoding rather than recomputing its correspondence to HTML, while service writes compute the canonical digest; active-session verification intentionally derives and hashes identity twice per navigation and cross-window activation may emit a redundant same-key state; real unmount stop is deferred one microtask to bridge React StrictMode, the temporary no-op pending-save flush must be replaced atomically when the editor mounts, and every draft-controller ownership replacement must await its lossless stop or pass through the serial coordinator; the security-sensitive stored-block sanitizer requires adversarial regression coverage as supported markup evolves; the module-level rejecting WordPress API handler intentionally governs every `apiFetch` consumer on the side-panel page; the pinned editor emits a stale React peer warning and 58 moderate production dependency advisories with no compatible automated fix; five high advisories exist only in the ESLint development chain and require an ESLint 10 compatibility assessment; npm reports an unapproved development install script for `@parcel/watcher`; real toolbar-to-native-side-panel host automation remains an explicit Playwright capability skip; BYOS production OAuth requires a registered client ID and interactive approval; public distribution remains blocked on a GPL-compatible project-license decision.
- Commands that currently pass: Clean `npm ci`; `npm run check` including format, lint, strict typecheck, 430 unit/component/repository/audit tests at 93.81% statement and 88.62% branch coverage, deterministic production build, 13-file extension package audit, genuine two-file isolated-editor production/CSP audit, and unpacked Chromium smoke with one pass; 37 focused draft-controller/coordinator cases, 24 focused editor-adapter cases, 165 focused identity/session/adapter/shell cases, 159 focused note-service and affected repository cases, 106 focused repository cases, and 84 focused page-identity cases; `git diff --check`.
- Commands that currently fail: `npm audit --omit=dev` exits nonzero for 58 moderate advisories propagated by the mandated isolated editor; full `npm audit` additionally reports five high development-only advisories through ESLint/minimatch/brace-expansion; the toolbar-to-native-side-panel Playwright case is intentionally skipped because Playwright cannot drive extension chrome.
- Context handoff summary: PP-004C2A passed corrective independent review and the full gate. `PageNoteDraftController` concurrently reads cached note/settings state, treats missing and tombstoned notes as empty, exposes immutable loading/load-error/ready states, suppresses identical editor callbacks, routes changed normalized emptiness through logical clear, serializes writes, ignores stale outcomes, retries failures, captures edits at flush settlement boundaries, and requires an awaited lossless stop before replacement ownership. `PendingPageSaveCoordinator` coalesces one serial navigation gate and drains replacement registrations before resolving. The real `PageNoteEditor` and draft controller remain intentionally absent from the current side-panel entry, which still supplies an explicit no-op flush. PP-004C2B must instantiate the real Chrome repositories/service/coordinator once, mount the cached draft only inside the page-keyed supported subtree, preserve StrictMode and retained-flush-error behavior, expose load/editor/save retry states, await teardown ownership correctly, and replace the no-op flush atomically.
