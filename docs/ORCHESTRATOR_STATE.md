# Orchestrator State

- Current milestone: M2 — identity migration.
- Current branch: `feat/chrome-notes` in `/home/ashfame/git-worktrees/pageperch-chrome-notes`.
- Last accepted commit: `0eedc3f` (`feat(identity): plan note migrations`); PP-005A2 is independently approved and included in the next atomic commit.
- Current task: PP-005B — compose exact-origin identity settings with validation, duplicate prevention, and the durable executor.
- Completed tasks: Imported and pushed the governing inputs; bootstrapped durable project state; delivered the reproducible audited MV3 foundation; implemented canonical page identity; implemented versioned local models/repositories; implemented the validated local-first note service; delivered the per-page restricted Gutenberg editor, autosave/navigation ownership, and exact-origin recent-note index; implemented the independently reviewed pure identity-migration planner with deterministic original-URL-headed stacking, dual-transition validation, fixed destination/tombstone records, fingerprints, and strict journal parsing; and implemented its independently reviewed durable executor with exact-origin CAS, semantic provenance, destination-before-tombstone phases, startup recovery, shared-lock mutation gating, sanitized worker failure containment, and real-graph recovery coverage for every phase.
- Next 3 tasks: Complete the identity settings UI; implement BYOS OAuth and ephemeral credential lifecycle; implement the S3 replica and synchronization engine.
- Key architecture decisions: Offline-first Chrome local storage is authoritative; page identity is exact-origin scoped; remote records are one deterministic S3 JSON object per page key; UI, application, persistence, and transport boundaries remain separate.
- Open risks and blockers: A crash between explicit physical note removal and its index cleanup can leave a filtered dangling membership until physical delete is retried; without Web Locks the mutation fallback is realm-local, but target Chrome exposes cross-context Web Locks; repository validation checks content-hash encoding rather than recomputing its correspondence to HTML, while service and migration writes compute the canonical digest; a storage implementation could theoretically persist phase data without the journal marker even though current Chromium storage batches one `set` call, so phase-ahead recovery remains a hardening consideration; pending journals duplicate the existing local BYOS access token inside requested settings and PP-006 disconnect must scrub it; active-session verification intentionally derives and hashes identity twice per navigation and cross-window activation may emit a redundant same-key state; cleanup failures deliberately retain the current draft registration until explicit retry, while runtime failures follow page transfer automatically; the root index has component/unit coverage but its complete packaged storage-change-to-new-tab flow remains PP-008; the security-sensitive stored-block sanitizer requires adversarial regression coverage as supported markup evolves; the module-level rejecting WordPress API handler intentionally governs every `apiFetch` consumer on the side-panel page; the pinned editor emits a stale React peer warning and 60 moderate production dependency advisories with no compatible automated fix; five high advisories exist only in the ESLint development chain and require an ESLint 10 compatibility assessment; npm reports an unapproved development install script for `@parcel/watcher`; the side-panel editor entry is 3,949,184 raw bytes and the complete JavaScript package is 1,061,472 gzip-compressed bytes against the 5 MiB budget; real toolbar-to-native-side-panel host automation remains an explicit Playwright capability skip; BYOS production OAuth requires a registered client ID and interactive approval; public distribution remains blocked on a GPL-compatible project-license decision.
- Commands that currently pass: Clean `npm ci`; `npm run check` including format, lint, strict typecheck, 591 unit/component/repository/audit tests at 92.50% statement and 88.11% branch coverage, deterministic production build, 15-file extension package audit, genuine two-file isolated-editor production/CSP audit, and unpacked Chromium smoke with two passes; 224 affected migration/repository/worker cases, 48 focused identity-migration planner/parser cases, 64 focused root-index/adapter/component cases, 43 focused ownership/mounted-shell cases, 37 focused draft-controller/coordinator cases, 24 focused editor-adapter cases, 52 focused active-session cases, 57 focused note-service cases, 106 focused repository cases, and 84 focused page-identity cases; `git diff --check`.
- Commands that currently fail: `npm audit --omit=dev` exits nonzero for 60 moderate advisories propagated by the mandated isolated editor; full `npm audit` additionally reports five high development-only advisories through ESLint/minimatch/brace-expansion; the toolbar-to-native-side-panel Playwright case is intentionally skipped because Playwright cannot drive extension chrome.
- Context handoff summary: PP-005A2 passed a corrective review cycle. A versioned journal stores the immutable PP-005A1 plan before writes; the Chrome adapter verifies semantic provenance, settings fingerprint, and complete exact-origin inventory under the same cross-context storage lock, then applies destinations, tombstones, settings, index repair, and journal removal in order. The service worker resumes any valid phase once per startup, ordinary note/settings mutations reject while any journal value exists, malformed or drifted state is preserved for recovery, and all four persisted phases converge to the exact preplanned records without new IDs or duplicate content. PP-005B only needs to validate and present rule edits, call `IdentityMigrationExecutor.start`, and refresh the displayed settings.
