# Orchestrator State

- Current milestone: M1 — offline note foundations.
- Current branch: `feat/chrome-notes` in `/home/ashfame/git-worktrees/pageperch-chrome-notes`.
- Last accepted commit: `a565860` (`feat(panel): wire active page session shell`); PP-004C1 is accepted and included in the next atomic commit.
- Current task: PP-004C2 — load cached notes, compose the accepted restricted editor, and replace the temporary no-pending-state flush boundary with real 750 ms local-first autosave.
- Completed tasks: Imported and pushed the governing inputs; bootstrapped durable project state; delivered the reproducible audited MV3 foundation; implemented canonical page identity; implemented versioned local models/repositories; implemented the validated local-first note service; implemented the UI-neutral active-page session controller; composed narrow Chrome tabs/settings adapters with a StrictMode-safe accessible React shell; and implemented an independently reviewed isolated-editor adapter with exact block modes, safe stored-content normalization, first-edit capture, enforced local-only WordPress API handling, mode-keyed remounts, and accessible automatic themes.
- Next 3 tasks: Compose cached note loading, editor-mode settings, a bounded draft coordinator, and real pending-save flushing; implement 750 ms debounced local autosave, clear semantics, and explicit status transitions; render the recent exact-origin index on root identities with safe new-tab navigation.
- Key architecture decisions: Offline-first Chrome local storage is authoritative; page identity is exact-origin scoped; remote records are one deterministic S3 JSON object per page key; UI, application, persistence, and transport boundaries remain separate.
- Open risks and blockers: A crash between explicit physical note removal and its index cleanup can leave a filtered dangling membership until physical delete is retried; without Web Locks the mutation fallback is realm-local, but target Chrome exposes cross-context Web Locks; repository validation checks content-hash encoding rather than recomputing its correspondence to HTML, while service writes compute the canonical digest; active-session verification intentionally derives and hashes identity twice per navigation and cross-window activation may emit a redundant same-key state; real unmount stop is deferred one microtask to bridge React StrictMode, and the temporary no-op pending-save flush must be replaced atomically when the editor mounts; the security-sensitive stored-block sanitizer requires adversarial regression coverage as supported markup evolves; the module-level rejecting WordPress API handler intentionally governs every `apiFetch` consumer on the side-panel page; the pinned editor emits a stale React peer warning and 58 moderate production dependency advisories with no compatible automated fix; five high advisories exist only in the ESLint development chain and require an ESLint 10 compatibility assessment; npm reports an unapproved development install script for `@parcel/watcher`; real toolbar-to-native-side-panel host automation remains an explicit Playwright capability skip; BYOS production OAuth requires a registered client ID and interactive approval; public distribution remains blocked on a GPL-compatible project-license decision.
- Commands that currently pass: Clean `npm ci`; `npm run check` including format, lint, strict typecheck, 393 unit/component/repository/audit tests at 93.66% statement and 89.65% branch coverage, deterministic production build, 13-file extension package audit, genuine two-file isolated-editor production/CSP audit, and unpacked Chromium smoke with one pass; 24 focused editor-adapter cases, 165 focused identity/session/adapter/shell cases, 159 focused note-service and affected repository cases, 106 focused repository cases, and 84 focused page-identity cases; `git diff --check`.
- Commands that currently fail: `npm audit --omit=dev` exits nonzero for 58 moderate advisories propagated by the mandated isolated editor; full `npm audit` additionally reports five high development-only advisories through ESLint/minimatch/brace-expansion; the toolbar-to-native-side-panel Playwright case is intentionally skipped because Playwright cannot drive extension chrome.
- Context handoff summary: PP-004C1 passed two corrective review cycles and main-thread full verification. `PageNoteEditor` uses the real isolated package and local CSS, recreates only exact safe text blocks from parsed or raw stored content, captures the otherwise-dropped first one-block mutation directly through pinned WordPress serialization, rejects unhandled WordPress API calls before `window.fetch`, suppresses initialization and duplicate saves, latches unsafe load failures, and internally remounts on editor-mode changes. The adapter is intentionally not imported by the current side-panel entry, so production composition still supplies an explicit no-op flush. PP-004C2 must load the cached note before mounting the adapter, stage and debounce serialized drafts through `NoteService`, replace the no-op flush in the same change, surface explicit local save states, and preserve the existing page-keyed subtree and transition contract.
