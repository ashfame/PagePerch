# Orchestrator State

- Current milestone: M1 — offline note foundations.
- Current branch: `feat/chrome-notes` in `/home/ashfame/git-worktrees/pageperch-chrome-notes`.
- Last accepted commit: `7a5c9a0` (`feat(panel): compose local note editor`); PP-004C3 is independently approved and included in the next atomic commit.
- Current task: PP-005A — define and implement deterministic exact-origin identity migration semantics before composing the options-page rule editor.
- Completed tasks: Imported and pushed the governing inputs; bootstrapped durable project state; delivered the reproducible audited MV3 foundation; implemented canonical page identity; implemented versioned local models/repositories; implemented the validated local-first note service; implemented the UI-neutral active-page session controller; composed narrow Chrome tabs/settings adapters with a StrictMode-safe accessible React shell; implemented an independently reviewed restricted editor adapter with exact block modes, safe stored-content normalization, first-edit capture, enforced local-only WordPress API handling, mode-keyed remounts, and accessible automatic themes; implemented an independently reviewed cached-note draft controller with exact 750 ms actual-change debounce, serial local persistence, retryable state, lossless awaited stop, and a generation-aware navigation flush coordinator; mounted the real repository/service/draft/editor graph behind an app-lifetime ownership coordinator with interruptible startup, failure-provenance-aware cleanup, actionable local states, aligned Gutenberg registries, and a real 280-pixel packaged supported-page smoke; and added an independently reviewed exact-origin root index with immutable revision-coalesced refreshes, stale lifecycle suppression, retained-data errors, local change subscriptions, accessible narrow UI, and canonical-only new-tab navigation.
- Next 3 tasks: Implement deterministic identity-rule migrations; compose exact-origin identity settings with validation and duplicate prevention; implement BYOS OAuth and ephemeral credential lifecycle.
- Key architecture decisions: Offline-first Chrome local storage is authoritative; page identity is exact-origin scoped; remote records are one deterministic S3 JSON object per page key; UI, application, persistence, and transport boundaries remain separate.
- Open risks and blockers: A crash between explicit physical note removal and its index cleanup can leave a filtered dangling membership until physical delete is retried; without Web Locks the mutation fallback is realm-local, but target Chrome exposes cross-context Web Locks; repository validation checks content-hash encoding rather than recomputing its correspondence to HTML, while service writes compute the canonical digest; active-session verification intentionally derives and hashes identity twice per navigation and cross-window activation may emit a redundant same-key state; cleanup failures deliberately retain the current draft registration until explicit retry, while runtime failures follow page transfer automatically; the root index has component/unit coverage but its complete packaged storage-change-to-new-tab flow remains PP-008; the security-sensitive stored-block sanitizer requires adversarial regression coverage as supported markup evolves; the module-level rejecting WordPress API handler intentionally governs every `apiFetch` consumer on the side-panel page; the pinned editor emits a stale React peer warning and 60 moderate production dependency advisories with no compatible automated fix; five high advisories exist only in the ESLint development chain and require an ESLint 10 compatibility assessment; npm reports an unapproved development install script for `@parcel/watcher`; the initial side-panel editor entry is 3,965,821 raw bytes but the complete JavaScript package is 1,053,097 gzip-compressed bytes against the 5 MiB budget; real toolbar-to-native-side-panel host automation remains an explicit Playwright capability skip; BYOS production OAuth requires a registered client ID and interactive approval; public distribution remains blocked on a GPL-compatible project-license decision.
- Commands that currently pass: Clean `npm ci`; `npm run check` including format, lint, strict typecheck, 484 unit/component/repository/audit tests at 93.15% statement and 87.82% branch coverage, deterministic production build, 14-file extension package audit, genuine two-file isolated-editor production/CSP audit, and unpacked Chromium smoke with two passes; 64 focused root-index/adapter/component cases, 43 focused ownership/mounted-shell cases, 37 focused draft-controller/coordinator cases, 24 focused editor-adapter cases, 52 focused active-session cases, 57 focused note-service cases, 106 focused repository cases, and 84 focused page-identity cases; `git diff --check`.
- Commands that currently fail: `npm audit --omit=dev` exits nonzero for 60 moderate advisories propagated by the mandated isolated editor; full `npm audit` additionally reports five high development-only advisories through ESLint/minimatch/brace-expansion; the toolbar-to-native-side-panel Playwright case is intentionally skipped because Playwright cannot drive extension chrome.
- Context handoff summary: PP-004 is complete and independently approved. The side-panel entry creates one settings repository shared by identity and draft loading, one note repository/service shared by drafts and the root index, one pending-save coordinator, one durable draft ownership coordinator, one local-storage change adapter, and one canonical-only tab opener. Supported identities load cached content and editor mode concurrently, then mount the restricted real Gutenberg editor and surface loading, local save, and retryable errors. Ownership lives outside React, stops before unregistering, interrupts pending startup without awaiting late outcomes, keeps failed cleanup flushable across remounts, and distinguishes cleanup failures from page-scoped runtime failures so navigation and disconnect cannot strand an owner. Exact-origin roots additionally subscribe before querying, publish immutable recency snapshots, coalesce bursts with revision gates, discard stale root work, retain prior entries during failures, and never interfere with editor ownership. Exact WordPress 24-series singleton pins plus one shared Vite/audit deduplication list eliminate the packaged editor’s split data registry and duplicate pattern store. PP-005 should first define a migration service that atomically applies exact-origin exclusion changes across stored notes and settings, deterministically merges collisions under source-URL headings, writes old-key tombstones, preserves representative URLs for removal, and is idempotent before the options UI invokes it.
