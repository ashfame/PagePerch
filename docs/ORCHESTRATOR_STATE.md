# Orchestrator State

- Current milestone: M3 — BYOS synchronization.
- Current branch: `feat/chrome-notes` in `/home/ashfame/git-worktrees/pageperch-chrome-notes`.
- Last accepted commit: `8b367db` (`feat(identity): execute note migrations`); PP-005B is independently approved and included in the next atomic commit.
- Current task: PP-006 — implement BYOS public-client OAuth PKCE, token/session persistence, ephemeral S3 credentials, disconnect, and options controls.
- Completed tasks: Imported and pushed the governing inputs; bootstrapped durable project state; delivered the reproducible audited MV3 foundation; implemented canonical page identity; implemented versioned local models/repositories and the validated local-first note service; delivered the per-page restricted Gutenberg editor, autosave/navigation ownership, and exact-origin recent-note index; implemented the pure identity-migration planner and durable startup-resumable executor; and completed the accessible options experience with editor-mode selection, read-only built-ins, canonical exact-origin custom rule validation, one-parameter migration actions, local/BYOS status, sanitized retryable errors, and a shared-lock atomic editor-mode patch that preserves the latest exclusions and BYOS metadata.
- Next 3 tasks: Implement BYOS OAuth and ephemeral credential lifecycle; implement the S3 replica and synchronization engine; add complete packaged integration coverage and release documentation.
- Key architecture decisions: Offline-first Chrome local storage is authoritative; page identity is exact-origin scoped; remote records are one deterministic S3 JSON object per page key; UI, application, persistence, and transport boundaries remain separate.
- Open risks and blockers: A crash between explicit physical note removal and its index cleanup can leave a filtered dangling membership until physical delete is retried; without Web Locks the mutation fallback is realm-local, but target Chrome exposes cross-context Web Locks; repository validation checks content-hash encoding rather than recomputing its correspondence to HTML, while service and migration writes compute the canonical digest; a storage implementation could theoretically persist phase data without the journal marker even though current Chromium storage batches one `set` call, so phase-ahead recovery remains a hardening consideration; pending journals duplicate the existing local BYOS access token inside requested settings and PP-006 disconnect must scrub it; a cross-context core-settings change can safely make a stale identity request fail until the options page reloads; active-session verification intentionally derives and hashes identity twice per navigation and cross-window activation may emit a redundant same-key state; cleanup failures deliberately retain the current draft registration until explicit retry, while runtime failures follow page transfer automatically; the root index has component/unit coverage but its complete packaged storage-change-to-new-tab flow remains PP-008; the security-sensitive stored-block sanitizer requires adversarial regression coverage as supported markup evolves; the pinned editor emits a stale React peer warning and 60 moderate production dependency advisories with no compatible automated fix; five high advisories exist only in the ESLint development chain and require an ESLint 10 compatibility assessment; npm reports an unapproved development install script for `@parcel/watcher`; the side-panel entry is 3,949,184 raw bytes and the complete JavaScript package is 1,064,168 gzip-compressed bytes against the 5 MiB budget; real toolbar-to-native-side-panel host automation remains an explicit Playwright capability skip; BYOS production OAuth requires a registered client ID and interactive approval; public distribution remains blocked on a GPL-compatible project-license decision.
- Commands that currently pass: Clean `npm ci`; `npm run check` including format, lint, strict typecheck, 621 unit/component/repository/audit tests at 92.72% statement and 88.05% branch coverage, deterministic production build, 17-file extension package audit, genuine two-file isolated-editor production/CSP audit, and unpacked Chromium smoke with two passes; 255 affected options/migration/repository/worker cases, 48 focused options/settings cases, 48 identity-migration planner/parser cases, 64 root-index/adapter/component cases, 43 ownership/mounted-shell cases, 37 draft-controller/coordinator cases, 24 editor-adapter cases, 52 active-session cases, 57 note-service cases, and 84 page-identity cases; `git diff --check`.
- Commands that currently fail: `npm audit --omit=dev` exits nonzero for 60 moderate advisories propagated by the mandated isolated editor; full `npm audit` additionally reports five high development-only advisories through ESLint/minimatch/brace-expansion; the toolbar-to-native-side-panel Playwright case is intentionally skipped because Playwright cannot drive extension chrome.
- Context handoff summary: PP-005B passed one corrective review cycle. The real options page loads versioned local settings, shows both editor modes, displays immutable built-in exclusions and canonical custom exact-origin rules, validates and normalizes one parameter addition/removal at a time, executes every identity change through the durable migration executor, refreshes after completion, preserves inputs on failure, and reports honest Local only or Local + BYOS status while PP-006 actions remain disabled. Editor mode uses a dedicated repository patch that reads the latest settings and writes only the new mode under the shared lock; a real-repository race proves worker recovery and newer BYOS metadata cannot be overwritten by a stale rendered snapshot.
