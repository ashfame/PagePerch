# Orchestrator State

- Current milestone: M7 — 0.1.2 editor canvas and rich-paste refinement, completed.
- Current branch: `feat/chrome-notes` in `/home/ashfame/git-worktrees/pageperch-chrome-notes`.
- Last accepted commit before this change: `715d737` (`chore(release): prepare 0.1.1 minor update`).
- Current task: Finalize the accepted PP-013 release through its source commit, annotated minor-update tag, remote branch/tag checks, and CI verification.
- Completed tasks: All implementation-plan delivery batches PP-001 through PP-010, post-plan refinement PP-011, the PP-012 `0.1.1` minor-update release, and PP-013 are complete, including the audited MV3 foundation, offline page identity and notes, compact full-height content-growing Gutenberg editor with safe rich-paste projection, default-off opt-in exact-origin index, collision-stacking identity exclusions, established-service BYOS OAuth/S3 integration, automatic queue-based reconciliation with passive status, real packaged-browser keyboard/history/persistence/index coverage, complete setup/privacy/operations documentation, and the deterministic recovery-safe release artifact/checksum flow.
- Next 3 tasks: Verify branch and annotated-tag CI for `v0.1.2`; manually verify trusted rich paste and the refined native side panel/toolbar host; manually verify an approved live BYOS client.
- Key architecture decisions: Offline-first Chrome local storage is authoritative; page identity is exact-origin scoped; remote records are one deterministic S3 JSON object per page key; the durable revision-specific queue entry is the sole per-page unsynced flag; synchronization is automatic with no manual remote control; the side-panel editor is control-free but keyboard complete; the root recent-note index is a default-off preference that performs no work while disabled; UI, application, persistence, and transport boundaries remain separate.
- Open risks and blockers: A crash between explicit physical note removal and its index cleanup can leave a filtered dangling membership until physical delete is retried; without Web Locks the mutation fallback is realm-local, but target Chrome exposes cross-context Web Locks; repository validation checks content-hash encoding rather than recomputing its correspondence to HTML, while service and migration writes compute the canonical digest; a storage implementation could theoretically persist phase data without the journal marker even though current Chromium storage batches one `set` call, so phase-ahead recovery remains a hardening consideration; a migration can begin after sync preparation and temporarily allow a pre-migration source upload between individually locked phases, with automatic completion follow-up and later full reconciliation providing convergence; disconnect resumes a legitimate pending identity journal before removing its duplicated token, while a malformed journal intentionally blocks automatic disconnect until recovery; an already-issued S3 request cannot be physically revoked at disconnect, but worker generation, connection-bound credential acquisition, settings CAS, and subsequent preflights prevent further stale operations or metadata; a cross-context core-settings change can safely make a stale identity request fail until the options page reloads; active-session verification intentionally derives and hashes identity twice per navigation and cross-window activation may emit a redundant same-key state; cleanup failures deliberately retain the current draft registration until explicit retry, while runtime failures follow page transfer automatically; the security-sensitive stored-block sanitizer requires adversarial regression coverage as supported markup evolves; the pinned editor emits a stale React peer warning and 60 moderate production dependency advisories with no compatible automated fix; five high advisories exist only in the ESLint development chain and require an ESLint 10 compatibility assessment; npm reports an unapproved development install script for `@parcel/watcher`; the side-panel entry is 3,954,488 raw bytes and the complete built JavaScript package is 1,143,244 gzip-compressed bytes against the 5 MiB budget; real toolbar-to-native-side-panel host automation remains an explicit Playwright capability skip; production OAuth requires an approved public client ID and interactive consent.
- Commands that currently pass: Clean `npm ci`; `npm run check` including format, lint, strict typecheck, 959 unit/component/repository/audit/release tests at 92.13% statement and 88.80% branch coverage, deterministic production build, 16-file extension package audit, genuine two-file isolated-editor production/CSP audit, and unpacked Chromium smoke with four passes and one explicit capability skip; genuine pinned Gutenberg paste-handler safety projection, packaged 16-pixel narrow/wide gutters, zero writing-flow padding, short-note viewport fill with visible status, keyboard-only blank editing, native undo/redo, document growth without an inner editor scroller, local persistence through panel/browser restart, and default-off then opted-in exact-origin index; 52 focused version/release cases; real concurrent packager rejection/coherence; two `npm run release:package` runs producing the identical 5,040,549-byte ZIP with SHA-256 `f0ea98eedcb16ef94bf516428aadcf145ff8ded3eae6d471ac77c6689b5e1e50`; strict `sha256sum --check`; `unzip -t`; embedded manifest version and exact artifact-cardinality inspection; `git diff --check`.
- Commands that currently fail: `npm audit --omit=dev` exits nonzero for 60 moderate advisories propagated by the mandated isolated editor; full `npm audit` additionally reports five high development-only advisories through ESLint/minimatch/brace-expansion; the toolbar-to-native-side-panel Playwright case is intentionally skipped because Playwright cannot drive extension chrome.
- Context handoff summary: `v0.1.1` is committed, tagged as a minor update, pushed, and independently green in branch/tag CI. PP-013 is implemented, independently approved after corrective security review, and locally release-gated at `0.1.2`: exact 16-pixel root block padding at every panel width, zero writing-flow block padding, styled supported HTML elements, semantic rich-HTML paste converted by Gutenberg into safe allowed blocks, persistence-time safe-schema projection, and a short-note canvas that fills the available viewport while retaining the passive note status. The official block-serialization default parser already exists in the pinned Gutenberg dependency graph; serialized note loading uses it through `@wordpress/blocks.parse`, while live clipboard HTML belongs to Gutenberg's `pasteHandler`/`rawHandler` path. Trusted clipboard behavior, native toolbar-to-side-panel behavior, and approved live BYOS OAuth/S3 remain the three intentional manual boundaries.
