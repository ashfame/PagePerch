# Orchestrator State

- Current milestone: M1 — offline note foundations.
- Current branch: `feat/chrome-notes` in `/home/ashfame/git-worktrees/pageperch-chrome-notes`.
- Last accepted commit: `6115e3c` (`feat(panel): add origin note index`); PP-005A1 is independently approved and included in the next atomic commit.
- Current task: PP-005A2 — durably persist and execute immutable identity-migration plans through CAS-checked resumable phases.
- Completed tasks: Imported and pushed the governing inputs; bootstrapped durable project state; delivered the reproducible audited MV3 foundation; implemented canonical page identity; implemented versioned local models/repositories; implemented the validated local-first note service; implemented the UI-neutral active-page session controller; composed narrow Chrome tabs/settings adapters with a StrictMode-safe accessible React shell; implemented an independently reviewed restricted editor adapter with exact block modes, safe stored-content normalization, first-edit capture, enforced local-only WordPress API handling, mode-keyed remounts, and accessible automatic themes; implemented an independently reviewed cached-note draft controller with exact 750 ms actual-change debounce, serial local persistence, retryable state, lossless awaited stop, and a generation-aware navigation flush coordinator; mounted the real repository/service/draft/editor graph behind an app-lifetime ownership coordinator with interruptible startup, failure-provenance-aware cleanup, actionable local states, aligned Gutenberg registries, and a real 280-pixel packaged supported-page smoke; added an independently reviewed exact-origin root index with immutable revision-coalesced refreshes, stale lifecycle suppression, retained-data errors, local change subscriptions, accessible narrow UI, and canonical-only new-tab navigation; and added an independently reviewed pure identity-migration planner with dual-transition identity validation, deterministic normalized merges/moves/tombstones, canonical CAS fingerprints, strict journal parsing, and real addition-to-removal lifecycle coverage.
- Next 3 tasks: Implement and interruption-test the durable migration executor; compose exact-origin identity settings with validation and duplicate prevention; implement BYOS OAuth and ephemeral credential lifecycle.
- Key architecture decisions: Offline-first Chrome local storage is authoritative; page identity is exact-origin scoped; remote records are one deterministic S3 JSON object per page key; UI, application, persistence, and transport boundaries remain separate.
- Open risks and blockers: A crash between explicit physical note removal and its index cleanup can leave a filtered dangling membership until physical delete is retried; without Web Locks the mutation fallback is realm-local, but target Chrome exposes cross-context Web Locks; repository validation checks content-hash encoding rather than recomputing its correspondence to HTML, while service and migration planner writes compute the canonical digest; the persisted migration parser proves structural/internal consistency rather than semantic provenance, so PP-005A2 must CAS the complete origin inventory and rederive rather than overwrite drifted state; active-session verification intentionally derives and hashes identity twice per navigation and cross-window activation may emit a redundant same-key state; cleanup failures deliberately retain the current draft registration until explicit retry, while runtime failures follow page transfer automatically; the root index has component/unit coverage but its complete packaged storage-change-to-new-tab flow remains PP-008; the security-sensitive stored-block sanitizer requires adversarial regression coverage as supported markup evolves; the module-level rejecting WordPress API handler intentionally governs every `apiFetch` consumer on the side-panel page; the pinned editor emits a stale React peer warning and 60 moderate production dependency advisories with no compatible automated fix; five high advisories exist only in the ESLint development chain and require an ESLint 10 compatibility assessment; npm reports an unapproved development install script for `@parcel/watcher`; the initial side-panel editor entry is 3,965,821 raw bytes but the complete JavaScript package is 1,053,097 gzip-compressed bytes against the 5 MiB budget; real toolbar-to-native-side-panel host automation remains an explicit Playwright capability skip; BYOS production OAuth requires a registered client ID and interactive approval; public distribution remains blocked on a GPL-compatible project-license decision.
- Commands that currently pass: Clean `npm ci`; `npm run check` including format, lint, strict typecheck, 532 unit/component/repository/audit tests at 93.04% statement and 88.21% branch coverage, deterministic production build, 14-file extension package audit, genuine two-file isolated-editor production/CSP audit, and unpacked Chromium smoke with two passes; 48 focused identity-migration planner/parser cases, 64 focused root-index/adapter/component cases, 43 focused ownership/mounted-shell cases, 37 focused draft-controller/coordinator cases, 24 focused editor-adapter cases, 52 focused active-session cases, 57 focused note-service cases, 106 focused repository cases, and 84 focused page-identity cases; `git diff --check`.
- Commands that currently fail: `npm audit --omit=dev` exits nonzero for 60 moderate advisories propagated by the mandated isolated editor; full `npm audit` additionally reports five high development-only advisories through ESLint/minimatch/brace-expansion; the toolbar-to-native-side-panel Playwright case is intentionally skipped because Playwright cannot drive extension chrome.
- Context handoff summary: PP-005A1 passed one corrective review cycle. Its pure planner validates exactly one semantic custom exclusion change, derives both transition identities from every affected record's stored representative URL, accepts a stored identity only when it matches one side, groups live records by requested identity, preserves order/content through valid Gutenberg merges, selects a moving representative for later removal, and emits normalized fixed-version destination/tombstone records. Public settings/note fingerprint functions and a strict dependency-free parser provide the persistence boundary; parser validation alone is not provenance, so PP-005A2 must store the plan before writes, compare the full exact-origin inventory plus settings fingerprint under the same cross-context Web Lock, apply destinations before tombstones and settings, persist/advance phases, resume matching work after interruption, and surface an explicit conflict without mutating when CAS state drifts.
