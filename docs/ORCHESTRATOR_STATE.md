# Orchestrator State

- Current milestone: M3 — BYOS synchronization.
- Current branch: `feat/chrome-notes` in `/home/ashfame/git-worktrees/pageperch-chrome-notes`.
- Last accepted commit: `8f77036` (`feat(settings): connect byos storage`); PP-007A is independently approved and included in the next atomic commit.
- Current task: PP-007B — implement the durable coalescing queue and deterministic local/remote reconciliation core.
- Completed tasks: Imported and pushed the governing inputs; bootstrapped durable project state; delivered the reproducible audited MV3 foundation; implemented canonical page identity, versioned local repositories, and the local-first note service; delivered the per-page restricted Gutenberg editor, autosave/navigation ownership, exact-origin recent-note index, identity-migration planner/executor/recovery, and full identity/settings options experience; implemented the independently reviewed extension-side BYOS client and accessible connection controls; and implemented the independently reviewed storage-neutral remote replica plus extension-side AWS SDK v3 S3 transport with exact deterministic keys, issued operation-scoped bucket/credentials, path-style configuration, paginated list/get/put behavior, strict remote validation, immutable ordering, large-note support, tombstone puts, and redacted failures.
- Next 3 tasks: Implement durable queueing and reconciliation; wire save/startup/panel/alarm/manual triggers and status; add complete packaged integration coverage and release documentation.
- Key architecture decisions: Offline-first Chrome local storage is authoritative; page identity is exact-origin scoped; remote records are one deterministic S3 JSON object per page key; UI, application, persistence, and transport boundaries remain separate.
- Open risks and blockers: A crash between explicit physical note removal and its index cleanup can leave a filtered dangling membership until physical delete is retried; without Web Locks the mutation fallback is realm-local, but target Chrome exposes cross-context Web Locks; repository validation checks content-hash encoding rather than recomputing its correspondence to HTML, while service and migration writes compute the canonical digest; a storage implementation could theoretically persist phase data without the journal marker even though current Chromium storage batches one `set` call, so phase-ahead recovery remains a hardening consideration; disconnect resumes a legitimate pending identity journal before removing its duplicated token, while a malformed journal intentionally blocks automatic disconnect until recovery; a cross-context core-settings change can safely make a stale identity request fail until the options page reloads; active-session verification intentionally derives and hashes identity twice per navigation and cross-window activation may emit a redundant same-key state; cleanup failures deliberately retain the current draft registration until explicit retry, while runtime failures follow page transfer automatically; the root index has component/unit coverage but its complete packaged storage-change-to-new-tab flow remains PP-008; the security-sensitive stored-block sanitizer requires adversarial regression coverage as supported markup evolves; the pinned editor emits a stale React peer warning and 60 moderate production dependency advisories with no compatible automated fix; five high advisories exist only in the ESLint development chain and require an ESLint 10 compatibility assessment; npm reports an unapproved development install script for `@parcel/watcher`; the side-panel entry is 3,949,180 raw bytes and the complete built JavaScript package is 1,069,489 gzip-compressed bytes against the 5 MiB budget; real toolbar-to-native-side-panel host automation remains an explicit Playwright capability skip; production OAuth requires an approved public client ID and interactive consent; public distribution remains blocked on a GPL-compatible project-license decision.
- Commands that currently pass: Clean `npm ci`; `npm run check` including format, lint, strict typecheck, 726 unit/component/repository/audit tests at 91.36% statement and 87.96% branch coverage, deterministic production build, 17-file extension package audit, genuine two-file isolated-editor production/CSP audit, and unpacked Chromium smoke with two passes; 35 focused S3 replica cases including a 9 MiB round trip, 78 focused BYOS client cases, 66 focused BYOS options cases, 255 affected options/migration/repository/worker cases, 48 options/settings cases, 48 identity-migration planner/parser cases, 64 root-index/adapter/component cases, 43 ownership/mounted-shell cases, 37 draft-controller/coordinator cases, 24 editor-adapter cases, 52 active-session cases, 57 note-service cases, and 84 page-identity cases; `git diff --check`.
- Commands that currently fail: `npm audit --omit=dev` exits nonzero for 60 moderate advisories propagated by the mandated isolated editor; full `npm audit` additionally reports five high development-only advisories through ESLint/minimatch/brace-expansion; the toolbar-to-native-side-panel Playwright case is intentionally skipped because Playwright cannot drive extension chrome.
- Context handoff summary: PP-007A is complete after independent review. It adds no server behavior: the extension creates one short-lived AWS SDK S3 client per operation from the existing coordinator’s temporary credentials, uses only the issued bucket, and destroys that client afterward. The reusable remote repository performs list/get/put only, paginates under the exact v1 note prefix with continuation-loop protection, treats genuine not-found reads as absence, rejects malformed UTF-8/JSON/schema/key mismatches and oversized data, returns frozen page-key-ordered records, and maps all transport/credential failures to stable redacted errors. Tombstones overwrite the same JSON object. PP-007B should add a versioned Chrome-local queue that stores page key plus current revision rather than record bodies, coalesces newer saves, uses bounded injected retry scheduling, and reconciles local/remote records by `savedAt` then `revisionId` without allowing stale queue completion to remove newer work.
