# Orchestrator State

- Current milestone: M1 — offline note foundations.
- Current branch: `feat/chrome-notes` in `/home/ashfame/git-worktrees/pageperch-chrome-notes`.
- Last accepted commit: `a16650a` (`docs(orchestration): bootstrap project state`); PP-001 is accepted and awaiting its commit.
- Current task: PP-002 — implement canonical page identity with exhaustive unit tests.
- Completed tasks: Imported and pushed the governing plan, BYOS spec, and logo; bootstrapped durable project state; delivered a reproducible Node 24/npm/React 18/Vite MV3 scaffold with exact permissions, generated icons, baseline accessible automatic-theme surfaces, CI, deterministic build checks, package/CSP auditing, and unpacked Chromium smoke coverage; added a genuine production bundle/audit of the pinned isolated editor and Gutenberg CSS.
- Next 3 tasks: Implement canonical page identity; implement versioned local repositories and `NoteService`; integrate the per-page restricted block editor and origin index.
- Key architecture decisions: Offline-first Chrome local storage is authoritative; page identity is exact-origin scoped; remote records are one deterministic S3 JSON object per page key; UI, application, persistence, and transport boundaries remain separate.
- Open risks and blockers: The pinned editor emits a stale React peer warning through `react-autosize-textarea`; its WordPress graph accounts for 58 moderate production dependency advisories with no compatible automated fix; five high advisories exist only in the ESLint development chain and require an ESLint 10 compatibility assessment; npm reports an unapproved development install script for `@parcel/watcher`; real toolbar-to-native-side-panel host automation is unavailable in Playwright and remains an explicit diagnostic skip; BYOS production OAuth requires a registered client ID and interactive approval; public distribution remains blocked on a GPL-compatible project-license decision.
- Commands that currently pass: Clean `npm ci`; `npm run check` including format, lint, strict typecheck, 33 unit/component/audit tests at 97.87% statement and 90.79% branch coverage, deterministic production build, 13-file extension package audit, genuine two-file isolated-editor production/CSP audit, and unpacked Chromium smoke with one pass; `git diff --check`.
- Commands that currently fail: `npm audit --omit=dev` exits nonzero for 58 moderate advisories propagated by the mandated isolated editor; full `npm audit` additionally reports five high development-only advisories through ESLint/minimatch/brace-expansion; the toolbar-to-native-side-panel Playwright case is intentionally skipped because Playwright cannot drive extension chrome.
- Context handoff summary: PP-001 passed three review cycles and main-thread clean verification. The editor smoke emits and audits approximately 4.4 MB minified JavaScript plus 412 KB CSS; Vite applies a narrow Lodash `Function('return this')()` to `globalThis` compatibility transform, and the lockfile overrides `@wordpress/core-data@7.24.0` to its matching `@wordpress/sync@1.24.0`. After committing PP-001, dispatch only PP-002 canonical identity to one worker, then one reviewer.
