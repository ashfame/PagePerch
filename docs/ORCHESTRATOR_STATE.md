# Orchestrator State

- Current milestone: M1 — offline note foundations.
- Current branch: `feat/chrome-notes` in `/home/ashfame/git-worktrees/pageperch-chrome-notes`.
- Last accepted commit: `299167e` (`feat(panel): add active page session controller`); PP-004B is accepted and included in the next atomic commit.
- Current task: PP-004C — mount the actual restricted editor, load cached notes, and replace the temporary no-pending-state flush boundary.
- Completed tasks: Imported and pushed the governing inputs; bootstrapped durable project state; delivered the reproducible audited MV3 foundation; implemented canonical page identity; implemented versioned local models/repositories; implemented the validated local-first note service; implemented the UI-neutral active-page session controller; and composed narrow Chrome tabs/settings adapters with a StrictMode-safe accessible React shell covering loading, unsupported, actionable error, retained flush-error, and keyed supported states.
- Next 3 tasks: Mount and restrict the actual isolated block editor with cached note loading and page-key isolation; implement 750 ms debounced local autosave, clear semantics, and explicit status transitions; render the recent exact-origin index on root identities with safe new-tab navigation.
- Key architecture decisions: Offline-first Chrome local storage is authoritative; page identity is exact-origin scoped; remote records are one deterministic S3 JSON object per page key; UI, application, persistence, and transport boundaries remain separate.
- Open risks and blockers: A crash between explicit physical note removal and its index cleanup can leave a filtered dangling membership until physical delete is retried; without Web Locks the mutation fallback is realm-local, but target Chrome exposes cross-context Web Locks; repository validation checks content-hash encoding rather than recomputing its correspondence to HTML, while service writes compute the canonical digest; active-session verification intentionally derives and hashes identity twice per navigation and cross-window activation may emit a redundant same-key state; real unmount stop is deferred one microtask to bridge React StrictMode, and the temporary no-op pending-save flush must be replaced atomically when the editor mounts; the real editor must confirm untouched and cleared serialization in PP-004; the pinned editor emits a stale React peer warning and 58 moderate production dependency advisories with no compatible automated fix; five high advisories exist only in the ESLint development chain and require an ESLint 10 compatibility assessment; npm reports an unapproved development install script for `@parcel/watcher`; real toolbar-to-native-side-panel host automation remains an explicit Playwright capability skip; BYOS production OAuth requires a registered client ID and interactive approval; public distribution remains blocked on a GPL-compatible project-license decision.
- Commands that currently pass: Clean `npm ci`; `npm run check` including format, lint, strict typecheck, 369 unit/component/repository/audit tests at 95.38% statement and 92.30% branch coverage, deterministic production build, 13-file extension package audit, genuine two-file isolated-editor production/CSP audit, and unpacked Chromium smoke with one pass; 165 focused identity/session/adapter/shell cases, 159 focused note-service and affected repository cases, 106 focused repository cases, and 84 focused page-identity cases; `git diff --check`.
- Commands that currently fail: `npm audit --omit=dev` exits nonzero for 58 moderate advisories propagated by the mandated isolated editor; full `npm audit` additionally reports five high development-only advisories through ESLint/minimatch/brace-expansion; the toolbar-to-native-side-panel Playwright case is intentionally skipped because Playwright cannot drive extension chrome.
- Context handoff summary: PP-004B passed corrective review and main-thread full verification. `ChromeActivePageTabs` exposes exact current-window queries plus symmetric raw-event subscriptions; `SettingsPageIdentityExclusions` deep-clones repository rules. `SidePanelApp` creates controllers only in committed effects, bridges the React 18 StrictMode probe with runtime/version-scoped microtask cleanup, replaces changed factories safely, suppresses post-unmount emissions, converts startup exceptions to actionable UI, and retains the exact keyed supported subtree beneath pending-flush errors. Production composition still supplies an explicit no-op flush because no editor state exists. PP-004C must replace that callback in the same change that introduces pending editor state, locally load notes before editor initialization, and preserve the keyed subtree/lifecycle contract.
