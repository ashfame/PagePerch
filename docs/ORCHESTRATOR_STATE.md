# Orchestrator State

- Current milestone: M1 — offline note foundations.
- Current branch: `feat/chrome-notes` in `/home/ashfame/git-worktrees/pageperch-chrome-notes`.
- Last accepted commit: `51bc702` (`feat(notes): add local-first note service`); PP-004A is accepted and included in the next atomic commit.
- Current task: PP-004B — compose the current-window Chrome adapter and React page-session shell before mounting the restricted editor.
- Completed tasks: Imported and pushed the governing inputs; bootstrapped durable project state; delivered the reproducible audited MV3 foundation; implemented canonical page identity; implemented versioned note/settings models, storage-neutral repositories, recovery-safe Chrome local adapters, exact-origin indexes, strict schema/key/URL validation, settings v0→v1 migration, defensive copies, serialized cross-context mutations, the validated local-first note service, and the UI-neutral active-page session controller with authoritative current-window routing, shared PP-002 identity verification, stale async suppression, and save-before-document-transition ordering.
- Next 3 tasks: Wire Chrome tabs/settings/identity into accessible loading, unsupported, error, and supported React session states; mount and restrict the actual per-page isolated block editor with debounced local autosave/status behavior; render the recent exact-origin index on root identities with safe new-tab navigation.
- Key architecture decisions: Offline-first Chrome local storage is authoritative; page identity is exact-origin scoped; remote records are one deterministic S3 JSON object per page key; UI, application, persistence, and transport boundaries remain separate.
- Open risks and blockers: A crash between explicit physical note removal and its index cleanup can leave a filtered dangling membership until physical delete is retried; without Web Locks the mutation fallback is realm-local, but target Chrome exposes cross-context Web Locks; repository validation checks content-hash encoding rather than recomputing its correspondence to HTML, while service writes compute the canonical digest; active-session verification intentionally derives and hashes identity twice per navigation, cross-window activation may emit a redundant same-key state, and the React consumer must retain the previous editor when a flush failure emits an identity-free error; the real editor must confirm untouched and cleared serialization in PP-004; the pinned editor emits a stale React peer warning and 58 moderate production dependency advisories with no compatible automated fix; five high advisories exist only in the ESLint development chain and require an ESLint 10 compatibility assessment; npm reports an unapproved development install script for `@parcel/watcher`; real toolbar-to-native-side-panel host automation remains an explicit Playwright capability skip; BYOS production OAuth requires a registered client ID and interactive approval; public distribution remains blocked on a GPL-compatible project-license decision.
- Commands that currently pass: Clean `npm ci`; `npm run check` including format, lint, strict typecheck, 341 unit/component/repository/audit tests at 94.87% statement and 92.04% branch coverage, deterministic production build, 13-file extension package audit, genuine two-file isolated-editor production/CSP audit, and unpacked Chromium smoke with one pass; 136 focused active-session and page-identity cases, 159 focused note-service and affected repository cases, 106 focused repository cases, and 84 focused page-identity cases; `git diff --check`.
- Commands that currently fail: `npm audit --omit=dev` exits nonzero for 58 moderate advisories propagated by the mandated isolated editor; full `npm audit` additionally reports five high development-only advisories through ESLint/minimatch/brace-expansion; the toolbar-to-native-side-panel Playwright case is intentionally skipped because Playwright cannot drive extension chrome.
- Context handoff summary: PP-004A passed four corrective review cycles and main-thread full verification. `derivePageIdentity` is now the single PP-002 derivation path and the default service delegates to it. `ActivePageSessionController` queries the current-window active tab, treats Chrome-wide activation as a refresh signal, confirms startup updates against current-window authority, fetches fresh defensive exclusions, compares injected identity output against shared derivation, suppresses stale generations, and serializes commits. A successful pending-save flush precedes every actual document departure, including rapid A→B→A→C races; same-key metadata updates do not flush. PP-004B should implement the narrow Chrome port and React lifecycle while keeping the prior keyed editor mounted if a pending-save flush fails.
